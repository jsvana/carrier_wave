# Confirmed QSOs handling in map view

**✅ DONE** - Updated QSOMapView.swift to check both `lotwConfirmed` OR `qrzConfirmed` for the confirmed filter

We need to ensure that both QRZ and LoTW confirmed QSOs are included in the map view. Take the union of the sets.
